<!-- harness:setup:begin -->
## Harness 工作流

`propose → review-proposal → plan → apply → review → archive`

常用入口：

- `/harness-workflow`：查看进行中 change。
- `/harness-propose`：创建需求。
- `/harness-knowledge`：检索或更新知识库。

## Superpowers 原则

- 操作前先检查适用 skill。
- 流程 skill 先于实现 skill。
- 实现遵循 TDD、逐任务评审和完成前验证。

## 知识库地图

- `docs/architecture/`：模块、分层、同步和隐性约定。
- `docs/standards/`：编码、测试、策略内容和安全规范。
- `docs/product/`：学习规则、范围与里程碑。
- `docs/superpowers/`：已批准设计和可执行计划。
- `openspec/`：需求变更、主规格和归档。
<!-- harness:setup:end -->

