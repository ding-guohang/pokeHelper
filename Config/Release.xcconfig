#include "Shared.xcconfig"

PC_CONTENT_CHANNEL = store

// A store release carries reviewed content only. The gate reads the
// channel back out of the built Info.plist and enforces that.
// The tournament push/fold packs are unverifiedDraft, so they are excluded
// from store here (kept in debug/dogfood); check-release-content.sh is the
// manifest-scan backstop if this line is ever wrong.
EXCLUDED_SOURCE_FILE_NAMES = DevStrategyPack.json tourn-hu-chip-ev-noante-*.json
